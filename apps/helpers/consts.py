"""Constants for JSON keys and values"""

class JsonKeys:
    STATUS = "status"
    MESSAGE = "message"
    CONFIG = "config"
    REQUEST = "request"
    DATA = "data"
    RESULT = "result"
    ERROR = "error"

class JsonValues:
    SUCCESS = "success"
    FAILURE = "failure"
    ERROR = "error"

class ApiTags:
    LABELS = "labels"
    MODELS = "models"
    INFERENCE = "inference"
    IMAGES = "images"
    SOURCES = "sources"
    TASKS = "tasks"